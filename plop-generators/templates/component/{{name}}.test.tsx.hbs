import { render } from '@testing-library/react';
import { {{name}} } from './{{name}}';

describe('<{{name}} />', () => {
  test('renders correctly', () => {
    const { baseElement } = render(<{{name}} title="{{name}}" />);
    expect(baseElement).toBeTruthy();
  });
});
